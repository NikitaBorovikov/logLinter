# 💥 LogLinter

**LogLinter** - это линтер для языка Go, предназначенный для анализа лог-записей в коде и проверки их на соответствие установленным правилам.

## Функционал
Линтер проверяет, что все лог-сообщения соответсвуют данным правилам:
- Начинаются со строчной буквы.
- Написаны на английском языке.
- Не содержат спецсимволы или эмодзи.
- Не содержат потенциально чувствительные данные. 

По умолчанию линтер поддерживает работу с логгерами `log/slog` и `go.uber.org/zap`. Добавить новые логгеры или изменить список потенциально чувствительных данных можно в `config/config.yaml` файле. 


Может использоваться как самостоятельная утилита, так и в качестве плагина для <a href="https://github.com/golangci/golangci-lint">golangci-lint</a>.

## Cистемные требования
- **Go:** 1.22+
- **ОС для использования в качестве утилиты:** любая.
- **ОС для использования в качестве плагина:** Linux или WSL2 (т.к. механизм Go Plugins официально не поддерживается на Windows и имеет ограничения на macOS).

## Установка
``` 
git clone https://github.com/NikitaBorovikov/logLinter
cd logLinter
```

## Использование как CLI
```
# Собрка
go build -o logLinter ./cmd/linter/main.go 

# Запуск
./logLinter <путь до проекта>
```

## Использование как плагина

### 1. Склонировать и собрать golangci-lint
```
git clone https://github.com/golangci/golangci-lint
cd golangci-lint/
CGO_ENABLED=1 make build
cd ..
```

### 2. Собрать плагин 
```
CGO_ENABLED=1 go build -buildmode=plugin plugin/main.go
```

### 3. Запустить
```
./golangci-lint/golangci-lint run -Eloglint <путь до проекта>
```

## Тестирование
Для запуска внутренних тестов линтера использовать команду:
```
go test ./...
```

## Примеры использования 
Примеры использования можно посмотреть <a href="https://github.com/NikitaBorovikov/logLinter/blob/master/example">тут</a>. 